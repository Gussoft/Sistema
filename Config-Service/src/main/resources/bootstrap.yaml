
server:
  port: 8094

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Gussoft/Sistema
          default-label: eureka
          search-paths: Config-Data
  application:
    name: config-service
  security:
    user:
      name: guss
      password: acm1pt