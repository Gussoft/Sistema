
server:
  port: 8094

spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/
        git:
          uri: https://github.com/Gussoft/Sistema
          default-label: config-server
          search-paths: config-data
      profile: native
  application:
    name: config-service